/* eslint-disable @nlib/no-globals, no-undef */
export const {
    Boolean,
    Number,
    Object,
    Array,
    Date,
    URL,
    JSON,
    Promise,
    requestAnimationFrame,
    cancelAnimationFrame,
    MutationObserver,
} = globalThis;
